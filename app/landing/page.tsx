// Server component placeholder. All A/B assignment and redirects are handled in `middleware.ts`.
// Keeping this page empty prevents client-side delays and analytics hits on `/landing`.
export default function LandingPlaceholder() {
  return null
}
